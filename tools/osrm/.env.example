# Directory containing the source PBF and generated OSRM files.
OSRM_DATA_DIR=./data

# Dataset basename: expects <name>.osm.pbf and generates <name>.osrm*.
OSRM_DATASET=turkey-latest

# Host port mapped to the container's fixed port 5000.
OSRM_HOST_PORT=5000
